global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'prometheus'
    static_configs:
      - targets: ['localhost:9090']

  - job_name: 'cadvisor'
    dns_sd_configs:
      - names:
        - 'tasks.mon-app_cadvisor'
        type: 'A'
        port: 8080

  - job_name: 'api'
    metrics_path: '/metrics'
    dns_sd_configs:
      - names:
        - 'tasks.mon-app_api'
        type: 'A'
        # Doit correspondre à PORT dans stack.yml (3000). 3001 est la valeur par
        # défaut du dev (main.ts) et ne vaut pas en production.
        port: 3000
